- name: Proxmox stuff
  hosts: localhost   # <-- UBAH INI
  connection: local  # <-- TAMBAHKAN INI
  gather_facts: no   # <-- TAMBAHKAN INI (praktik yang baik)
  vars:
    name: ubuntu
  tasks:
    - set_fact:
        yeppers: "freshbakevm-ubuntu"
    - proxmox_kvm:
        api_user    : root@pam
        api_token_id: 1f2f8879-6ee8-4141-988e-ddfeec2954e9
        api_host    : 192.168.210.137
        clone       : UbuntuServTemplate
        name        : "{{ yeppers }}"
        node        : pve
        storage     : local-lvm
        format      : qcow2
        timeout     : 500
    - name: start vm
      proxmox_kvm:
        api_user: root@pam
        api_token_id: 1f2f8879-6ee8-4141-988e-ddfeec2954e9
        api_host: 192.168.210.137
        name: "{{ yeppers }}"
        node: pve
        state: started